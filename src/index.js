/**
 * Import the new site-tagline variation
 */
import './variation';
import './filters/attributes';
import './filters/controls';

/**
 * Import the old block for backwards compatibility.
 * This keeps existing blocks working while encouraging migration.
 */
import './blocks/random-description'; 